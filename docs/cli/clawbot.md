---
summary: "CLI reference for `lala clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `lala clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `lala clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `lala clawbot qr` (same behavior as [`lala qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `lala clawbot qr` -> `lala qr`
