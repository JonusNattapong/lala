apiVersion: v1
kind: ConfigMap
metadata:
  name: lala-config
  labels:
    app: lala
data:
  lala.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.lala/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "Lala Assistant",
            "workspace": "~/.lala/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # Lala Assistant

    You are a helpful AI assistant running in Kubernetes.
