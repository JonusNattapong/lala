apiVersion: v1
kind: Service
metadata:
  name: lala
  labels:
    app: lala
spec:
  type: ClusterIP
  selector:
    app: lala
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
