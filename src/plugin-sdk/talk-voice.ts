// Narrow plugin-sdk surface for the bundled talk-voice plugin.
// Keep this list additive and scoped to symbols used under extensions/talk-voice.

export type { LalaPluginApi } from "../plugins/types.js";
